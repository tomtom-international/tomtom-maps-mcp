# Source code (not needed at runtime - dist/ has the compiled output)
src/
tsconfig.json
rolldown.config.js

# Development files
.github/
.history/
.claude/
.agents/
.vscode/
docs/
tests/
ui/
coverage/

# Config files not needed at runtime
.env
.env.*
biome.json
vitest.config.*
.gitignore
.gitattributes
.dockerignore
Dockerfile
docker-compose.*
entrypoint.sh
CLAUDE.md
CONTRIBUTING.md
CHANGELOG.md

# Package manager metadata (the bundle ships dist/ + prod node_modules)
pnpm-lock.yaml
pnpm-workspace.yaml

# Build scripts (not needed at runtime)
scripts/

# Dev dependencies artifacts
*.tsbuildinfo

# OS files
.DS_Store
Thumbs.db

# Generated bundles
*.mcpb
