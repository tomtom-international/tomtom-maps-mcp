{
  "//": "The root tsconfig only includes `src/**/*` (it drives the build, with rootDir: src). The evals live outside that and pull in devDependencies the build never sees, so they get their own project. Run via `pnpm type-check:evals`.",
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "rootDir": "..",
    "noEmit": true,
    "declaration": false,
    "sourceMap": false
  },
  "include": ["../evals/**/*", "../src/**/*"],
  "exclude": ["../node_modules", "../dist"]
}
